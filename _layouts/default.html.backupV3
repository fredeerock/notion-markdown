<!DOCTYPE html>
<html lang="{{ site.lang | default: "en-US" }}">
  <head>
    <meta charset='utf-8'>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{ '/assets/css/style.css?v=' | append: site.github.build_revision | relative_url }}">
    {% include head-custom.html %}

{% seo %}
  </head>

  <body>

    <header>
      <div class="container">
        <a id="a-title" href="{{ '/' | relative_url }}">
          <h1>{{ site.title | default: site.github.repository_name }}</h1>
        </a>
        <h2>{{ site.description | default: site.github.project_tagline }}</h2>

                  <!-- Navigation -->
          <nav style="margin-top: 1em;">
            <a href="{{ '/' | relative_url }}" {% if page.url == '/' %}style="font-weight: bold; color: #fff;"{% else %}style="color: #ccc;"{% endif %}>Home</a>
            {% assign nav_pages = site.pages | where: "type", "Page" %}
            {% for p in nav_pages %}
              <a href="{{ p.url | relative_url }}" {% if page.url == p.url %}style="font-weight: bold; color: #fff;"{% else %}style="color: #ccc;"{% endif %} style="margin-left: 1em;">{{ p.title }}</a>
            {% endfor %}
          </nav>

      </div>
    </header>

    <div class="container">
      <section id="main_content">
        {{ content }}
      </section>
    </div>
  </body>
</html>